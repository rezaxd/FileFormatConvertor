# django app which converts .jpg files into .png files & .mp4 files into .mp3 files!
## more format in future :D


## Dependencies:
apt-get install ffmpeg libavcodec-extra
pip install -r requirements.txt
